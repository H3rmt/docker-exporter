name: Test Go

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
      - name: Install dependencies
        run: go get ./...
      - name: Build
        run: go build -v ./...